{
    "name": "Choose base currency",
    "description": "Quote against a different currency than Euro by setting the base parameter",
    "documentation": "http://fixer.io/",
    "fields": [
        {
            "label": "Choose base currency",
            "properties": {
                "parameter": "baseCurrency",
                "fieldType": "text",
                "helptext": "Rates are quoted against the Euro by default. Quote against a different currency by setting the base parameter."
            },
            "readonly": false,
            "required": true,
            "visible": true
        }
    ],
    "method": "GET",
    "endpoint": "/latest?base={field.baseCurrency}",
    "request": {},
    "response": {
        "success": "",
        "error": ""
    }
}